import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    width: "80%",
    borderWidth: 5,
    height: 70,
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 12,
    marginBottom: 30
  },

  h2: {
    fontSize: 28,
    fontWeight: "bold",
  },
});
